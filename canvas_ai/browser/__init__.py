"""Browser-session auth: drive your own logged-in Canvas session.

For districts that disable personal access tokens. You log in once (through
whatever SSO/MFA your school uses); the session persists in a local browser
profile and is reused for Canvas REST API calls via your session cookies.
"""
